# Tinyfish Next.js App

Minimal Next.js scaffold.

Quick start:

```bash
cd web
npm install
npm run dev
```

This only creates the scaffold. Run the commands above to install dependencies and start the dev server.
